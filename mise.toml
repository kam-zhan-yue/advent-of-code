experimental_monorepo_root = true

[env]
MISE_EXPERIMENTAL=1

# For now define tools in the monorepo. As time passes and new versions come out, it will be necessary to move to subfolders
[tools]
python = "3.14.0"
cmake = "4.2.0"
rust = "1.91.1"
zig = "0.15.2"
elixir = "1.19.4"
go = "1.25.5"
deno = "2.6.0"
lua = "5.4.8"
